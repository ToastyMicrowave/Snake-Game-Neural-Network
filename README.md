# Snake Game Neural Network
This project is a reinforcement learning experiment where a neural network learns to play Snake using Python.  
It uses NumPy for matrix operations and a custom training loop for forward/backward propagation.  
